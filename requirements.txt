# Offline-only stack — all libraries run locally with no internet after install.
# ANN uses sklearn MLPRegressor (not TensorFlow / PyTorch).
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
threadpoolctl>=3.0
xgboost>=2.0
matplotlib>=3.7
